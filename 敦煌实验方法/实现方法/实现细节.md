## 分工安排
	王晓政:等效降雨方案多地区适配+降雨日照耦合方法
	沛铨：冻融次数确定与等效方法调研
	品言：温度模拟方案
		1、
## 环境配置
npm --version：8.19.4
node -v：v16.20.2

1、接受参数：
	region：只能选择传入的第一个，单地区查询。
	datarange：[string| number, string | number]，作为起始时间和结束时间
	envFactors：数组，指的是进行模拟时考虑的因素列表
	antiqueType：对应哪一类的文物
2、
## 数据类
新建一个类ExperimentInput
属性：
	weather_data:存储读取的环境数据List[Dict]
方法:
	初始化方法：默认为None
	读取数据方法：input_data = sys.stdin.read()
		data = json.loads(input_data)
		使用这个data作为参数。
	判断是否有数据的方法。
	获取结构信息方法：获得现在每个数据有哪些字段？
	获取数据的起止时间：
	

	
	